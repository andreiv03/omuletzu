{
  "name": "omuletzu",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "node -r dotenv/config ./dist/index.js",
    "dev": "npm run build && npm run start",
    "deploy-commands": "npm run build && node -r dotenv/config ./dist/utils/deploy-commands.js",
    "heroku-postbuild": "npm install && npm run build"
  },
  "dependencies": {
    "discord-api-types": "^0.37.1",
    "discord.js": "^14.1.2"
  },
  "devDependencies": {
    "@types/node": "^18.6.5",
    "dotenv": "^16.0.1",
    "typescript": "^4.7.4"
  }
}
