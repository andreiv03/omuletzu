{
  "dependencies": {
    "discord-api-types": "^0.37.24",
    "discord.js": "^14.7.1",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "name": "omuletzu",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "deploy": "npm run build && node -r dotenv/config ./dist/utils/deploy-commands.js",
    "dev": "nodemon",
    "start": "node -r dotenv/config ./dist/index.js"
  }
}
